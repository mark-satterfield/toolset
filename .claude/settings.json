{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,os; inp=os.environ.get('CLAUDE_TOOL_INPUT','{}'); d=json.loads(inp); fp=d.get('file_path',''); sys.exit(0) if not fp.endswith('SKILL.md') else None; c=open(fp).read(); missing=[f for f in ['name:','description:'] if f not in c]; print('SKILL.md frontmatter OK') if not missing else sys.exit('SKILL.md missing required frontmatter fields: '+', '.join(missing))\""
          }
        ]
      }
    ]
  },
  "permissions": {
  }
}
