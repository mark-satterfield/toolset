# Commands

Place workflow entry-point commands here.

The Command team in `../references/sdlc-agent-roster.csv` is the first source for command definitions:

- `ss-master-orchestrator`
- `ss-iteration-supervisor`
- `ss-beads-coordinator`
- `ss-repository-manager`
- `ss-patrol-agent`

Commands should launch or coordinate the relevant agents without duplicating the full agent instructions.

