/**
 * @mark-satterfield/pmo
 *
 * Claude Code plugin — not a Node.js module.
 * Plugin functionality is implemented in:
 *   commands/   — slash command markdown files
 *   references/ — reference files
 *   skills/     — auto-activating skill files
 *   .claude-plugin/plugin.json — manifest
 */
export const metadata = {
  name: "pmo",
  version: "0.1.0",
  description: "Project Management Office — ADRs, specs, drift detection, issue planning, implementation workflows, and documentation generation.",
};