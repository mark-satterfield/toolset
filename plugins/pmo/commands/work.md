---
description: Pick up tracker issues and implement them in parallel using git worktrees.
argument-hint: "[SPEC-XXXX | issue numbers | (empty = propose from backlog)] [--max-agents N] [--draft] [--dry-run] [--no-tests]"
---

Invoke the `pmo:work` skill with the provided arguments: $ARGUMENTS
