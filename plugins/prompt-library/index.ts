/**
 * @mark-satterfield/plugin-prompt-library
 *
 * Claude Code plugin — not a Node.js module.
 * Plugin functionality is implemented in:
 *   commands/   — slash command markdown files
 *   agents/     — subagent definitions
 *   skills/     — auto-activating skill files
 *   hooks/      — event hook configuration
 *   .claude-plugin/plugin.json — manifest
 */
export const metadata = {
  name: "prompt",
  version: "0.1.0",
  description: "Full-featured prompt management for Claude Code",
};
