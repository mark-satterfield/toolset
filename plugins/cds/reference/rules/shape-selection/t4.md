---
kind: shape-selection-rule
name: t4
section: T4
family: landing
status: stable
signals: [item_count, has_visual_per_item, copy_density_per_item, taxonomy_type]
table:
  - { when: "item_count == 3 && copy_density_per_item == short && !has_visual_per_item", primary: card-grid, alternates: [tagged-card-grid] }
  - { when: "item_count == 4 && taxonomy_type != null", primary: tagged-card-grid, alternates: [card-grid] }
  - { when: "item_count >= 2 && item_count <= 4 && has_visual_per_item && copy_density_per_item != short", primary: alternating-rows, alternates: [tabbed-panels] }
  - { when: "item_count >= 5 && item_count <= 6 && has_visual_per_item", primary: tabbed-panels, alternates: [alternating-rows] }
  - { when: "item_count >= 10", primary: card-carousel, alternates: [tag-columns] }
  - { when: "taxonomy_type != null && !has_visual_per_item", primary: tag-columns, alternates: [card-grid] }
default: card-grid
---

# Shape selection — T4 Capability Showcase

The pick balances count against weight: few light items grid, a taxonomy tags the grid, substantial copy-plus-visual pairs alternate in rows, a mid-sized visual set tabs, a large set carousels, and categorical pills without visuals fall into tag columns. Rows are ordered — the first matching predicate wins. Candidates are tried in order (primary, then alternates, then `default`) against the page constraints; if every candidate is rejected, the composer falls back to agent-generated layout and records that in the decisions sidecar.
