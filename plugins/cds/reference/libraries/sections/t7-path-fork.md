---
kind: section
name: path-fork
id: T7
family: landing
aliases: [path fork, choose your path, self-serve vs assisted, buy or talk to sales]
status: stable
mode: dynamic
content_contract:
  item_count: int
  emphasis: "balanced | sequential | contrast"
theme: scheduled
composition_notes: []
---

# T7 — Path Fork

Binary fork in the funnel: self-serve versus assisted. `item_count` is 2 by definition — the fork presents exactly two paths. `emphasis` names how the two paths relate: `balanced` peers, a `sequential` reading of one band after another, or an attribute-by-attribute `contrast`.

The shape pick consults `item_count` and `emphasis` via `rules/shape-selection/t7.md`.
