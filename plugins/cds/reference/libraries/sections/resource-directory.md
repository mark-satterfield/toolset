---
kind: section
name: resource-directory
aliases: [resources, resource library, docs and guides, content library]
status: stable
content_contract:
  has_source_tag_per_item: bool
  has_group_headings: bool
theme: scheduled
composition_notes: []
---

# Resource Directory

Docs, blog posts, and guides — a content surface that deepens engagement. The Shape pick branches on `has_source_tag_per_item` (items belong to typed sources such as docs or blog) and on `has_group_headings` (the items arrive already divided into named groups) via `rules/shape-selection/resource-directory.md`.
