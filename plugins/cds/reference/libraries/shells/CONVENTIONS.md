# Shell library conventions

A library shell entry is the templated Shell (the Building Blocks "Shell Template") — its persistent Sections pinned, one placeholder content slot; the composer instantiates it into a Shell that can be persisted for reuse.
