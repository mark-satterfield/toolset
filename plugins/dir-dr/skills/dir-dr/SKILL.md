---
name: dir-dr
description: >-
  Directory Doctor — expert at organizing any directory by identifying what it is
  and does at the domain level, researching external best practices and norms for
  that kind of thing, and measuring the gap between current state and where it
  should be. Works on any domain: code repos, legal filings, infrastructure,
  documentation libraries, business file hierarchies, mixed-purpose directories.
  Handles multi-domain directories where different content types coexist under
  separate norms. Trigger on: "scan my project", "clean up this folder",
  "restructure", "naming conventions", "what's wrong with my layout", "stale files",
  "dir-dr", "directory doctor", or any request about how files and folders are
  organized, named, or should be arranged.
---

Loads `plugins/dir-dr/commands/dir-dr.md` for full instructions. This skill auto-activates
on directory structure questions and delegates to the command for the complete workflow.
